#! /bin/bash

BIN=/usr/bin/chromium-real

# Cleanup
if ! pgrep chromium > /dev/null;then
  rm -f $HOME/.config/chromium/Singleton*
fi

${BIN} --password-store=basic --no-sandbox --ozone-platform=wayland --force-renderer-accessibility --test-type "$@"
